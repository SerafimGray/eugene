// import { throttle } from 'lodash';
import "../css/normalize.css";
import "../css/_main.css";

/* onScroll()
document.addEventListener('scroll', throttle(onScroll, 200))

function onScroll() {
  const about = document.getElementById('about')
  if (elementInViewport(about)) {
    const diplomaAboutWire = document.getElementById('to-about')
    animate([about, diplomaAboutWire])
  }
}

function elementInViewport(element) {
  const rect = element.getBoundingClientRect();
  const isInViewport = window.innerHeight - rect.top > 0
  return isInViewport
}

function animate(elements) {
  elements.forEach(element => element.classList.add('anim'))
} */
